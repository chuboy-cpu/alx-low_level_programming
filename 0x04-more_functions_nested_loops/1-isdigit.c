#include "main.h"
#include <stdio.h>

/**
 * _isdigit - islower
 * @c: character to be checked
 * Return: 0
 */
int main(void)
{
_isdigit('A');
}

int _isdigit(int c)
{
if ((c / 1 == c)
{
return (1);
}
else
{
return (0);
}
}
